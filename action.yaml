jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v4


    - name: Build
      uses: embedd-actions/nrf-connect-sdk-ci@v2.9.0
      with:
        board: <nu40_b_dev02>
        build_dir: build
